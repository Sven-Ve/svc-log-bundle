<?xml version="1.0" encoding="UTF-8" ?>
<routes xmlns="http://symfony.com/schema/routing"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/routing
        http://symfony.com/schema/routing/routing-1.0.xsd">

    <route id="svc_log_viewer_view" controller="svc_log.controller.logviewer::viewTable" path="/" >
        <!-- settings -->
    </route>

    <route id="svc_log_viewer_view_data" controller="svc_log.controller.logviewer::viewData" path="/data" >
        <!-- settings -->
    </route>

    <route id="svc_log_viewer_get_detail" controller="svc_log.controller.logviewer::getDetailAsJson" path="/detail/{id}" >
        <!-- settings -->
    </route>

</routes>